# Dependencias del bot IQOPT (estrategia 'seq': modelo secuencial LSTM/Transformer).
#
#   python -m venv .venv314
#   .venv314\Scripts\python.exe -m pip install -r requirements.txt   (Windows)
#   .venv314/bin/python -m pip install -r requirements.txt           (Linux)
#
# Se usan RANGOS, no versiones exactas: anclarlas a las del entorno de desarrollo
# (Python 3.14) hacia imposible instalar en un servidor con 3.10, porque numpy 2.4
# exige >=3.11. Con rangos, pip resuelve lo mas nuevo que sirva en cada Python.

# La API de IQ Option NO esta en PyPI: se instala desde git.
git+https://github.com/iqoptionapi/iqoptionapi.git@8a903cc094a74af1ed935a56a2d6b5a9ed3319d7

# EXACTO a proposito: versiones posteriores rompen iqoptionapi.
websocket-client==0.56.0

requests>=2.28
numpy>=1.24,<3

# ---------------------------------------------------------------------------
# HASTA AQUI es todo lo que necesita el bot para OPERAR.
#
# Si en models/ hay un .npz (lo genera exportar_npz.py), la inferencia va en numpy
# puro y torch NO hace falta. Recomendado en servidores: en Windows Server torch
# falla a menudo al cargar c10.dll (WinError 1114) por el runtime de Visual C++ o
# por un CPU sin AVX2, y son 122 MB para unas pocas multiplicaciones de matrices.
#
# Lo de abajo es SOLO para ENTRENAR. En un servidor que unicamente opera, instalar:
#   pip install -r requirements.txt --no-deps   (o comentar estas dos lineas)
# ---------------------------------------------------------------------------

# Entrenamiento del modelo secuencial (train_seq_save.py).
torch>=2.2

# Baseline HistGradientBoosting de eurusd_seq.py, para comparar contra el LSTM.
scikit-learn>=1.3
