# Entornos virtuales
.venv/
.venv314/
env/
venv/

# Cache de Python
__pycache__/
*.py[cod]
*.pyo

# Logs de ejecución
logs/*.log
*.log
bot_run.log
bot_err.log
rsi_iq.log
bot_demo*.log
mejora_out.txt

# Caches de datos OHLC (grandes; se regeneran con los descargadores)
cache_ohlc/
cache_ohlc_1m/
cache_ohlc_5m/
cache_ohlc_30m/
cache_closes/
cache_feats/
cache_feats_directo/

# Salidas regenerables de los backtests (grandes; se rehacen con los scripts)
bt_horas_senales.json
bt_probe.json

# Herramientas locales
.opencode/

# Archivos temporales / pendientes generados en runtime
operaciones_temp.txt
operaciones_pendientes.txt

# Base de datos SQLite (estado de runtime; se regenera con inicializar_db)
*.db
*.db-wal
*.db-shm

# Sistema
Thumbs.db
.DS_Store

# Configuración local de Claude Code (permisos por máquina)
.claude/settings.local.json

# Credenciales (NUNCA subir): email/password de IQ en texto plano
config.json
.env
secrets.local.json
models/*.pt
models/*.pt.json
cache_ohlc_5m_v2/
factores_*.npz
