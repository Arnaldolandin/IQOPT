# Dependencias del bot IQOPT (estrategia 'seq': modelo secuencial LSTM/Transformer).
#
#   python -m venv .venv314
#   .venv314\Scripts\python.exe -m pip install -r requirements.txt   (Windows)
#   .venv314/bin/python -m pip install -r requirements.txt           (Linux)
#
# Se usan RANGOS, no versiones exactas: anclarlas a las del entorno de desarrollo
# (Python 3.14) hacia imposible instalar en un servidor con 3.10, porque numpy 2.4
# exige >=3.11. Con rangos, pip resuelve lo mas nuevo que sirva en cada Python.

# La API de IQ Option NO esta en PyPI: se instala desde git.
git+https://github.com/iqoptionapi/iqoptionapi.git@8a903cc094a74af1ed935a56a2d6b5a9ed3319d7

# EXACTO a proposito: versiones posteriores rompen iqoptionapi.
websocket-client==0.56.0

requests>=2.28
numpy>=1.24,<3

# Inferencia y entrenamiento. La rueda CPU basta: el modelo es chico (LSTM de 1 capa,
# 48 unidades) y en GPU no se gana nada relevante.
# Los .pt se guardan como state_dict (tensores + metadatos), formato estable entre
# versiones: un modelo entrenado con 2.13 carga sin problema en 2.2+.
torch>=2.2

# Solo para eurusd_seq.py (baseline HistGradientBoosting contra la que se compara el
# LSTM). El bot NO lo necesita para operar: se puede omitir en el servidor.
scikit-learn>=1.3
